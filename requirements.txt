# Direct runtime dependencies for the hate-speech pipeline.
# Pinned loosely so reviewers can install on Python 3.9+ without resolver
# headaches. The full transitive set lives in the venv; this file is what
# you would commit if you wanted to rebuild from scratch.

# -- Tier-1 inference --
torch>=2.0
transformers>=4.40
numpy>=1.24
scikit-learn>=1.3        # classical LR / SVM baselines (saved as .pkl)

# -- Data plane --
kafka-python>=2.0
elasticsearch==8.10.2    # match the docker-compose image version
pandas>=2.0

# -- Ingestion adapters --
pytchat>=0.5             # YouTube live chat
requests>=2.31           # weather tool + ad-hoc HTTP

# -- Tier-2 agents --
ollama>=0.3              # local LLM client (uses gpt-oss:120b-cloud)

# -- Retrieval-Augmented Moderation (Qdrant + sentence-transformers) --
qdrant-client>=1.10      # vector DB client (matches docker-compose Qdrant 1.10.1)
sentence-transformers>=2.7   # embedding model (default: all-MiniLM-L6-v2, ~80 MB)

# -- Statistical evaluation --
scipy>=1.10              # McNemar's chi^2 in notebooks/evaluation.ipynb §4.4

# -- Config / env --
python-dotenv>=1.0
pyyaml>=6.0              # config/taxonomy.yaml, config/rag.yaml

# -- Operator dashboard + analytics (Streamlit + Plotly) --
streamlit>=1.30          # scripts/operator_dashboard.py, scripts/analytics_dashboard.py
plotly>=5.18             # charts for scripts/ui/dashboard.py
streamlit-autorefresh>=1.0   # optional live auto-refresh for the dashboard

# -- Additional ingestion adapters --
praw>=7.7                # Reddit comment streaming (src/ingestion/adapters/reddit_adapter.py)
# Twitch IRC chat is read via stdlib socket; no extra library needed.
